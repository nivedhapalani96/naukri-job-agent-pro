<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background-color: #4CAF50; color: white; padding: 20px; border-radius: 5px 5px 0 0; }
        .content { background-color: #f9f9f9; padding: 20px; border: 1px solid #ddd; }
        .job-title { font-size: 24px; font-weight: bold; margin-bottom: 10px; }
        .company { font-size: 18px; color: #666; margin-bottom: 10px; }
        .location { color: #888; margin-bottom: 15px; }
        .score { font-size: 20px; font-weight: bold; color: #4CAF50; margin: 15px 0; }
        .reasons { background-color: #e8f5e9; padding: 15px; border-radius: 5px; margin: 15px 0; }
        .reasons ul { margin: 10px 0; padding-left: 20px; }
        .button { display: inline-block; padding: 12px 24px; background-color: #4CAF50; color: white; text-decoration: none; border-radius: 5px; margin: 15px 0; }
        .notes { background-color: #fff3cd; padding: 15px; border-radius: 5px; margin: 15px 0; }
        .footer { text-align: center; color: #888; font-size: 12px; margin-top: 20px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>New Job Match Found!</h1>
        </div>
        <div class="content">
            <div class="job-title">{{ job.title }}</div>
            <div class="company">{{ job.company }}</div>
            <div class="location">{{ job.location }}</div>
            
            <div class="score">Score: {{ score }}/100</div>
            
            <div class="reasons">
                <strong>Match Reasons:</strong>
                <ul>
                    {% for reason in reasons %}
                    <li>{{ reason }}</li>
                    {% endfor %}
                </ul>
            </div>
            
            {% if notes %}
            <div class="notes">
                <strong>Your Profile Notes:</strong>
                <pre style="white-space: pre-wrap;">{{ notes }}</pre>
            </div>
            {% endif %}
            
            <a href="{{ job_url }}" class="button" target="_blank">View Job on Naukri</a>
            
            <div class="footer">
                <p>Posted: {{ job.posted_text }}</p>
                <p>This is an automated notification from Naukri Job Agent.</p>
            </div>
        </div>
    </div>
</body>
</html>

